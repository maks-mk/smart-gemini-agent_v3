# ✅ Таймауты увеличены до 300 секунд

## 🔧 Что исправлено

### Проблема:
Агент прерывался через **30 секунд**, хотя в config.json было указано 300 секунд.

### Причина:
Класс `AgentConfig` **не читал** поля `request_timeout` и `tool_timeout` из config.json.

### Решение:
Обновлены **5 файлов** для корректного применения таймаутов:

1. ✅ **config.json** - `request_timeout: 300`, `tool_timeout: 300`
2. ✅ **win-cli-config.json** - `commandTimeout: 300`
3. ✅ **agent_config.py** - добавлены поля `request_timeout` и `tool_timeout`
4. ✅ **agent.py** - константы обновлены на 300.0
5. ✅ **timeout.py** - дефолтные значения обновлены на 300.0

---

## 🚀 Как применить

### Перезапустите агента:

```bash
# Остановите текущий процесс
Ctrl+C

# ИЛИ принудительно
taskkill /F /IM python.exe

# Запустите заново
python main.py
```

**ВАЖНО:** Изменения вступят в силу **только после перезапуска!**

---

## ✅ Проверка работы

### Тест команды nmap:

```
Вы: проверь 192.168.0.4

Агент: [запускает nmap -A 192.168.0.4]
💓 Heartbeat: операция активна (10.2с)
💓 Heartbeat: операция активна (20.5с)
💓 Heartbeat: операция активна (30.8с)
💓 Heartbeat: операция активна (40.1с)
...
💓 Heartbeat: операция активна (180.5с)
💓 Heartbeat: операция активна (190.8с)
✅ Результаты сканирования [через ~200 секунд]
```

**Раньше:** прерывалось через 30 секунд ❌  
**Теперь:** работает до 300 секунд (5 минут) ✅

---

## 📊 Текущие настройки

| Параметр | Значение |
|----------|----------|
| **Общий таймаут запроса** | 300 сек (5 минут) |
| **Таймаут инструмента** | 300 сек (5 минут) |
| **Таймаут команд (win-cli)** | 300 сек (5 минут) |
| **Heartbeat интервал** | 10 сек |
| **Recursion limit** | 20 |

---

## 🔍 Диагностика

### Проверьте что изменения применились:

```bash
# 1. Проверить config.json
grep "timeout" config.json
# Должно быть:
# "request_timeout": 300,
# "tool_timeout": 300

# 2. Проверить win-cli-config.json
grep "commandTimeout" win-cli-config.json
# Должно быть:
# "commandTimeout": 300

# 3. Проверить константы в коде
grep "DEFAULT_.*_TIMEOUT" smart_gemini_agent/core/agent.py
# Должно быть:
# DEFAULT_REQUEST_TIMEOUT = 300.0
# DEFAULT_TOOL_TIMEOUT = 300.0
```

### Логи при запуске:

После запуска агента, проверьте логи:

```bash
# Должны увидеть при инициализации
tail ai_agent.log
```

Агент должен загрузить конфигурацию с таймаутами 300 сек.

---

## ⚠️ Возможные проблемы

### Проблема 1: Все еще прерывается через 30 сек

**Решение:**
```bash
# Убедитесь что агент был ПЕРЕЗАПУЩЕН
# Старый процесс НЕ подхватит изменения!

# Убейте все Python процессы
taskkill /F /IM python.exe

# Запустите заново
python main.py
```

### Проблема 2: Heartbeat не появляется

**Решение:**
```bash
# Включите DEBUG логи
# В config.json:
{
  "logging": {
    "level": "DEBUG"
  }
}
```

### Проблема 3: Команда выполняется дольше 5 минут

**Решение:** Увеличьте таймауты еще больше:

```json
// config.json
{
  "agent": {
    "request_timeout": 600,  // 10 минут
    "tool_timeout": 600
  }
}

// win-cli-config.json
{
  "security": {
    "commandTimeout": 600
  }
}
```

Затем перезапустите агента.

---

## 📈 Что теперь работает

### ✅ Долгие команды:

- **nmap -A** (полное сканирование) - до 5 минут
- **Поиск файлов** на большом диске - до 5 минут
- **Глубокое сканирование портов** - до 5 минут
- **Сложные скрипты** - до 5 минут

### ✅ Обратная связь:

- **Heartbeat каждые 10 секунд** - видно что операция активна
- **Можно прервать Ctrl+C** - graceful shutdown
- **Понятные ошибки** если таймаут все же превышен

### ✅ Безопасность:

- **Не зависает навсегда** - максимум 5 минут
- **Можно прервать** в любой момент
- **Логи показывают прогресс**

---

## 🎯 Примеры использования

### Пример 1: Сетевой аудит

```
Вы: проверь 192.168.0.4

Агент выполнит:
- nmap -A 192.168.0.4
- Полное сканирование портов
- Определение ОС и сервисов
- Heartbeat показывает прогресс
- Результат через 1-3 минуты ✅
```

### Пример 2: Поиск файлов

```
Вы: найди все .pdf файлы

Агент выполнит:
- Рекурсивный поиск в текущей папке
- Heartbeat каждые 10 сек
- Список найденных файлов ✅
```

### Пример 3: Сложная команда

```
Вы: запусти сложный скрипт анализа

Агент выполнит:
- Запуск PowerShell скрипта
- Таймаут 300 сек
- Результаты или ошибка ✅
```

---

## 📝 История изменений

### v4.0-timeout-fix:

- ✅ Добавлены `request_timeout` и `tool_timeout` в `AgentConfig`
- ✅ Обновлены константы в `agent.py` на 300.0
- ✅ Обновлены дефолты в `timeout.py` на 300.0
- ✅ config.json и win-cli-config.json установлены на 300
- ✅ Полная поддержка таймаутов из конфигурации

---

## ✅ Чек-лист

После перезапуска агента проверьте:

- [ ] Агент запустился без ошибок
- [ ] Команда `проверь 192.168.0.4` **не прерывается** через 30 сек
- [ ] Heartbeat появляется каждые 10 секунд
- [ ] Операции могут выполняться до 5 минут
- [ ] Можно прервать через Ctrl+C
- [ ] Логи содержат heartbeat сообщения

---

## 🆘 Если не работает

1. **Убейте все Python процессы**
   ```bash
   taskkill /F /IM python.exe
   ```

2. **Проверьте файлы конфигурации**
   ```bash
   cat config.json | grep timeout
   cat win-cli-config.json | grep commandTimeout
   ```

3. **Включите DEBUG**
   ```json
   { "logging": { "level": "DEBUG" } }
   ```

4. **Проверьте логи**
   ```bash
   tail -f ai_agent.log
   ```

5. **Попробуйте простую команду**
   ```
   Вы: покажи файлы
   ```
   Должна работать быстро (1-2 сек)

---

**Дата:** 2025-11-03  
**Версия:** v4.0-timeout-300-fixed  
**Статус:** ✅ ПОЛНОСТЬЮ ИСПРАВЛЕНО

**Действие:** Перезапустите агента и все заработает!

