# ⚡ Быстрое решение проблемы зависания

## ✅ Изменения УЖЕ применены автоматически

Я исправил основные причины зависания:

### 1. ✅ Уменьшен таймаут команд
- **win-cli-config.json**: `commandTimeout: 300` → `30` секунд
- **Было:** команды могли выполняться до 5 минут!
- **Стало:** максимум 30 секунд

### 2. ✅ Добавлены глобальные таймауты
- **config.json**: добавлены `request_timeout: 120` и `tool_timeout: 30`
- Операции теперь автоматически прерываются при превышении времени

### 3. ✅ Обновлена обработка в agent.py
- Добавлена обертка с таймаутами для всех операций
- Добавлена обработка Ctrl+C
- Уменьшен `recursion_limit` с 50 до 20
- Добавлен heartbeat для мониторинга

### 4. ✅ Новый модуль timeout.py
- Создан `smart_gemini_agent/utils/timeout.py`
- Обеспечивает защиту от зависания на всех уровнях

### 5. ✅ Обновлена модель
- Переход на `gemini-2.0-flash-exp` (быстрее и стабильнее)

---

## 🚀 Что делать дальше

### Шаг 1: Перезапустите агента

```bash
python main.py
```

### Шаг 2: Тестирование

Попробуйте запрос, который раньше вызывал зависание:

```
пользователь: запусти команду и покажи результат
```

Теперь операция:
- ✅ Завершится максимум за 30 секунд
- ✅ Покажет heartbeat каждые 10 секунд
- ✅ Можно прервать через Ctrl+C

### Шаг 3: Если еще зависает

#### A. Отключите проблемные MCP серверы

Откройте `mcp.json` и отключите:

```json
{
  "playwright": { "enabled": false },
  "chrome-mcp": { "enabled": false },
  "mcp-telegram": { "enabled": false }
}
```

#### B. Включите DEBUG логи

В `config.json`:
```json
{
  "logging": {
    "level": "DEBUG"
  }
}
```

Затем смотрите лог в реальном времени:
```bash
# Windows
Get-Content ai_agent.log -Wait -Tail 50

# Linux/Mac
tail -f ai_agent.log
```

#### C. Уменьшите таймауты еще больше

В `config.json`:
```json
{
  "agent": {
    "request_timeout": 60,
    "tool_timeout": 15
  }
}
```

---

## 🔍 Диагностика текущего состояния

### Проверьте что изменения применились:

```bash
# Проверить таймаут команд
grep "commandTimeout" win-cli-config.json
# Должно быть: "commandTimeout": 30

# Проверить таймауты агента
grep "timeout" config.json
# Должно быть: "request_timeout": 120, "tool_timeout": 30

# Проверить модель
grep "model_name" config.json
# Должно быть: "model_name": "gemini-2.0-flash-exp"
```

---

## ⚠️ Экстренное прерывание

### Если агент завис СЕЙЧАС:

#### Windows:
```powershell
# Нажмите Ctrl+C в окне где запущен агент

# Если не помогло - принудительно:
taskkill /F /IM python.exe
taskkill /F /IM node.exe
```

#### Linux/Mac:
```bash
# Нажмите Ctrl+C

# Если не помогло:
pkill -9 python
pkill -9 node
```

---

## 📊 Что изменилось в поведении

### ДО исправлений:
```
❌ Команда выполняется... (зависание до 5 минут)
❌ Нет обратной связи
❌ Ctrl+C не работает
❌ Приходится убивать процесс
```

### ПОСЛЕ исправлений:
```
✅ Команда выполняется...
💓 Heartbeat: операция активна (10.2с)
💓 Heartbeat: операция активна (20.5с)
✅ Готово! (27.3с)

ИЛИ если превышен таймаут:

⏱️ Timeout: операция заняла более 120с
Fix: упростите запрос | отключите медленные инструменты
```

---

## 🎯 Типичные сценарии

### Сценарий 1: Долгая команда
**Запрос:** `найди все .pdf файлы на диске C:`

**Раньше:** зависание на 5+ минут  
**Теперь:** прервется через 120 секунд с понятным сообщением

### Сценарий 2: Проблемный инструмент
**Запрос:** `открой сайт через playwright`

**Раньше:** зависание если сайт не отвечает  
**Теперь:** таймаут через 30 секунд

### Сценарий 3: Зацикливание
**Запрос:** (любой, вызывающий зацикливание)

**Раньше:** бесконечное повторение  
**Теперь:** остановка после 5 повторов с предупреждением

---

## 📝 Мониторинг в реальном времени

### Новые heartbeat сообщения в логах:

```log
2025-11-03 10:15:23 - DEBUG - 💓 Heartbeat: 3 элементов, 12.5с
2025-11-03 10:15:33 - DEBUG - 💓 Heartbeat: 5 элементов, 22.8с
2025-11-03 10:15:43 - DEBUG - 💓 Heartbeat: 8 элементов, 33.1с
```

Эти сообщения показывают, что агент работает, а не завис.

---

## 🆘 Если проблема не решена

1. **Соберите логи:**
   ```bash
   # Последние 100 строк
   tail -n 100 ai_agent.log > debug_log.txt
   ```

2. **Проверьте активные процессы:**
   ```bash
   # Windows
   tasklist | findstr -i "python node"
   
   # Linux/Mac
   ps aux | grep -E "python|node"
   ```

3. **Создайте минимальную конфигурацию:**
   В `mcp.json` оставьте только:
   ```json
   {
     "filesystem": { "enabled": true }
   }
   ```

4. **Полный перезапуск:**
   ```bash
   # Убейте все процессы
   taskkill /F /IM python.exe
   taskkill /F /IM node.exe
   
   # Подождите 5 секунд
   
   # Запустите заново
   python main.py
   ```

---

## ✅ Проверка успешности исправлений

Попробуйте эти тесты:

### Тест 1: Быстрая команда
```
пользователь: покажи файлы в текущей папке
```
Должна выполниться за 1-2 секунды

### Тест 2: Средняя команда
```
пользователь: найди все .py файлы и посчитай их
```
Должна выполниться за 5-15 секунд с heartbeat

### Тест 3: Прерывание
```
пользователь: найди все файлы на диске
```
Нажмите Ctrl+C через 5 секунд - должно прерваться сразу

### Тест 4: Таймаут
```
пользователь: выполни очень долгую команду
```
Должно прерваться через 120 секунд с сообщением о таймауте

---

## 📄 Полная документация

- **TROUBLESHOOTING_HANGS_RU.md** - подробное описание всех причин
- **OPTIMIZATION_REPORT.md** - отчет по оптимизации токенов
- **OPTIMIZATION_GUIDE_RU.md** - руководство по экономии

---

**Версия:** v4.0-fixed  
**Дата:** 2025-11-03  
**Статус:** ✅ Исправления применены автоматически

