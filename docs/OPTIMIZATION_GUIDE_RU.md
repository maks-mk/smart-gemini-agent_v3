# 📘 Краткое руководство по оптимизации токенов

## 🚀 Быстрый старт

### 1. Применить оптимизированный промпт (уже готово ✅)

Файл `prompt_optimized.md` уже обновлен и сокращен на **60%**.

Убедитесь, что в `config.json` указан правильный промпт:

```json
{
  "files": {
    "prompt_file": "prompt_optimized.md"
  }
}
```

### 2. Перезапустить агента

```bash
python main.py
```

---

## 💰 Текущая экономия

- **Системный промпт:** -60% токенов
- **Описания инструментов:** -40% токенов  
- **Контекст запросов:** -70% токенов
- **Сообщения об ошибках:** -75% токенов

**Общая экономия: ~55% на каждый запрос**

---

## 🎯 Дополнительная оптимизация (опционально)

### Вариант 1: Context Caching для Gemini 1.5+

Если используете Gemini 1.5 Pro/Flash, включите кэширование:

```python
# В .env добавьте
ENABLE_CONTEXT_CACHING=true
```

**Экономия:** до 75% на повторяющихся запросах

### Вариант 2: Переход на Gemini Flash 2.0

Более быстрая и дешевая модель:

```json
// config.json
{
  "agent": {
    "model_name": "gemini-2.0-flash-exp"
  }
}
```

**Экономия:** в 5-10 раз дешевле

### Вариант 3: Снижение температуры

Для детерминированных ответов (меньше токенов):

```json
{
  "agent": {
    "temperature": 0.1
  }
}
```

---

## 📊 Примеры экономии

### До оптимизации:
```
Запрос: "создай файл test.txt"

Системный промпт: 380 токенов
Инструменты: 500 токенов
Контекст: 100 токенов
─────────────────────────────
ИТОГО INPUT: 980 токенов
```

### После оптимизации:
```
Запрос: "создай файл test.txt"

Системный промпт: 150 токенов
Инструменты: 300 токенов
Контекст: 30 токенов
─────────────────────────────
ИТОГО INPUT: 480 токенов

ЭКОНОМИЯ: 500 токенов (51%)
```

---

## 🔍 Мониторинг расхода

### Отслеживание в логах

Включите подробное логирование:

```json
// config.json
{
  "logging": {
    "level": "DEBUG"
  }
}
```

В логах будет видно количество используемых инструментов и контекста.

### Статистика Gemini API

Проверяйте использование в [Google AI Studio](https://aistudio.google.com/):
- API Keys → Usage
- Смотрите на Input/Output tokens

---

## ⚡ Быстрые команды

### Проверить текущий промпт
```bash
cat prompt_optimized.md
```

### Сравнить с оригиналом
```bash
diff docs/prompt.md prompt_optimized.md
```

### Проверить конфигурацию
```bash
cat config.json | grep prompt_file
```

---

## ❓ FAQ

**Q: Повлияет ли оптимизация на качество ответов?**  
A: Нет, сохранена вся функциональность. Убраны только избыточные слова.

**Q: Можно ли вернуть старый промпт?**  
A: Да, в `config.json` измените `prompt_file` на `"docs/prompt.md"`

**Q: Какая модель самая дешевая?**  
A: Gemini Flash 2.0 ($0.075 за 1M input токенов)

**Q: Сколько можно сэкономить в месяц?**  
A: При 1000 запросов/день: ~$9.74/год или ~$0.81/месяц

---

## 📞 Поддержка

Полный отчет: `OPTIMIZATION_REPORT.md`

Вопросы и предложения: создайте Issue в репозитории проекта.

