# Миграция на Smart Gemini Agent v4.0

## Что нового в v4.0

### ✨ Основные улучшения

1. **Поддержка HTTP/SSE транспортов для MCP серверов**
   - Server-Sent Events (SSE)
   - Streamable HTTP
   - Смешанная конфигурация (stdio + HTTP)

2. **Оптимизированная обработка ошибок**
   - Улучшенная валидация конфигурации
   - Детальное логирование транспортов
   - Лучшие сообщения об ошибках

3. **Модернизированный код**
   - Type hints для всех функций
   - Разделение ответственности
   - Улучшенная читаемость

4. **Расширенная документация**
   - Полное руководство по HTTP транспортам
   - Примеры конфигураций
   - Примеры серверов

## Изменения в конфигурации

### Новые возможности mcp.json

Теперь вы можете использовать HTTP транспорты:

```json
{
  "example-sse-server": {
    "transport": "sse",
    "url": "http://localhost:3000/sse",
    "enabled": true
  },
  "example-http-server": {
    "transport": "streamable-http",
    "url": "https://api.example.com/mcp",
    "headers": {
      "Authorization": "Bearer YOUR_API_KEY"
    },
    "enabled": true
  }
}
```

### Старая конфигурация остается совместимой

Все существующие stdio конфигурации продолжат работать без изменений:

```json
{
  "filesystem": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path"],
    "transport": "stdio",
    "enabled": true
  }
}
```

## Установка новых зависимостей

Обновите зависимости для поддержки HTTP транспортов:

```bash
pip install -r requirements.txt
```

Новые пакеты:
- `httpx>=0.24.0` - HTTP клиент
- `aiohttp>=3.8.0` - Async HTTP
- `sse-starlette>=1.6.0` - SSE поддержка
- `websockets>=11.0` - WebSocket (для будущего)

## Пошаговая миграция

### Шаг 1: Обновление кода

Если вы клонировали репозиторий, просто выполните:

```bash
git pull origin main
```

### Шаг 2: Обновление зависимостей

```bash
pip install --upgrade -r requirements.txt
```

### Шаг 3: Проверка конфигурации

Запустите агента для проверки валидности конфигурации:

```bash
python main.py
```

При запуске вы увидите статистику транспортов:

```
📊 Статистика транспортов: stdio=3, sse=0, http=0
  💻 filesystem: stdio транспорт (npx)
  💻 duckduckgo: stdio транспорт (uvx)
```

### Шаг 4: Добавление HTTP серверов (опционально)

Если вы хотите использовать HTTP транспорты, добавьте их в `mcp.json`:

```json
{
  "your-http-server": {
    "transport": "sse",
    "url": "http://your-server:port/path",
    "enabled": true
  }
}
```

## Обратная совместимость

### ✅ Совместимо:
- Все существующие stdio серверы
- config.json формат
- Переменные окружения
- API агента

### ⚠️ Устаревшие функции:
Нет устаревших функций в этой версии.

### ❌ Несовместимые изменения:
Нет несовместимых изменений. Версия 4.0 полностью обратно совместима с v3.0.

## Тестирование после миграции

### Проверка базовой функциональности

1. Запустите агента:
```bash
python main.py
```

2. Проверьте инициализацию всех серверов в логах

3. Выполните простой запрос для проверки работоспособности

### Проверка HTTP транспортов

Если вы добавили HTTP серверы:

1. Убедитесь, что сервер доступен:
```bash
curl http://your-server:port/path
```

2. Проверьте логи агента на наличие ошибок подключения

3. Выполните запрос, использующий инструменты HTTP сервера

## Откат на v3.0

Если возникли проблемы, вы можете откатиться:

```bash
git checkout v3.0
pip install -r requirements.txt
```

## Производительность

### Оптимизации в v4.0:

- ⚡ Улучшенная валидация конфигурации
- ⚡ Оптимизированная загрузка логирования
- ⚡ Лучшее управление ресурсами
- ⚡ Детальное логирование без избыточности

### Рекомендации:

1. Используйте `DEBUG` уровень логирования только для отладки
2. Отключайте неиспользуемые серверы в `mcp.json`
3. Для HTTP серверов настройте адекватные таймауты

## Получение помощи

### Документация

- [HTTP Transport Guide](docs/HTTP_TRANSPORT_GUIDE.md) - полное руководство по HTTP транспортам
- [README](README.md) - основная документация
- [OPTIMIZATION_GUIDE](docs/OPTIMIZATION_GUIDE_RU.md) - руководство по оптимизации

### Логи

При возникновении проблем проверьте `ai_agent.log`:

```bash
tail -f ai_agent.log
```

Для детального логирования установите в `config.json`:

```json
{
  "logging": {
    "level": "DEBUG"
  }
}
```

### Проблемы

Если вы обнаружили баг или у вас есть вопросы:

1. Проверьте существующие issues в репозитории
2. Создайте новый issue с деталями проблемы
3. Приложите логи и конфигурацию (без секретов!)

## Известные ограничения

1. **Подстановка переменных окружения в mcp.json** - пока не реализована автоматически. Используйте скрипты для генерации конфигурации.

2. **WebSocket транспорт** - планируется в будущих версиях.

3. **Автоматическое переподключение** - при потере связи с HTTP сервером требуется перезапуск агента.

## Дорожная карта

Планируемые улучшения для следующих версий:

- [ ] Автоматическое переподключение к HTTP серверам
- [ ] WebSocket транспорт
- [ ] Подстановка переменных окружения в mcp.json
- [ ] Метрики производительности HTTP транспортов
- [ ] Балансировка нагрузки между серверами

## Обратная связь

Ваше мнение важно! Пожалуйста, поделитесь опытом миграции и предложениями по улучшению.

---

**Smart Gemini Agent v4.0** - Более мощный, гибкий и готовый к будущему! 🚀

