# Core dependencies
python-dotenv>=1.0.0

# AI and LangChain
langchain-google-genai>=1.0.0
langchain-openai>=0.1.0
langchain-mcp-adapters>=0.1.0
langchain-core>=0.1.0
langgraph>=0.1.0

# Rich terminal interface
rich>=13.0.0

# Data handling
pydantic>=2.0.0

# HTTP/Network support for SSE and Streamable HTTP transports
httpx>=0.24.0           # Modern HTTP client with HTTP/2 support
aiohttp>=3.8.0          # Async HTTP client/server
sse-starlette>=1.6.0    # Server-Sent Events support
websockets>=11.0        # WebSocket support for potential future use

# Optional MCP servers (install separately)
# duckduckgo-mcp-server  # pip install duckduckgo-mcp-server
# mcp-server-fetch       # pip install mcp-server-fetch
# blender-mcp            # pip install blender-mcp
# excel-mcp-server       # pip install excel-mcp-server